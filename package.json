{
  "name": "optimis-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-all": "concurrently \"npm:start-backend\" \"npm:start-frontend\" \"npm:start-python\"",
    "start-backend": "npm --prefix backend start",
    "start-frontend": "npm --prefix frontend run dev",
    "start-python": "python3 backend/python/app.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

